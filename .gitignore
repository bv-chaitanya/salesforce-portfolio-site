# Salesforce / SFDX
.sf/
.sfdx/
.localdevserver/
deploy-options.json

# Resumes contain personal contact details — never publish with the repo
resumes/

# Node / tooling
node_modules/
npm-debug.log*

# OS
.DS_Store

# Logs
*.log

# Profile photo lives ONLY in the org (deployed static resource) — never in the public repo
force-app/main/default/staticresources/profilePhoto.*
force-app/main/default/staticresources/favicon.*
